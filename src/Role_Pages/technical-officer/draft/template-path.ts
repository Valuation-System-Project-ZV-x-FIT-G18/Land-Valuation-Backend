//08
import { join } from 'path'

/** Canonical land-only Word report template copied into dist by Nest assets. */
export const LAND_ONLY_REPORT_TEMPLATE_PATH = join(
  __dirname,
  'templates',
  'valuation-report-land-only.docx',
)
